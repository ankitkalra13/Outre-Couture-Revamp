# Frontend Environment Configuration Example
# Copy this file to .env.local and update values for local development
# Production environment variables are set in Vercel dashboard

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Note: Only variables prefixed with NEXT_PUBLIC_ are accessible in the browser
# Sensitive data should NEVER be stored in frontend environment files
